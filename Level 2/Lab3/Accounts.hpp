#ifndef ACCOUNTSH
#define ACCOUNTSH

#include <memory>
#include <vector>
#include "LongTermSavingsAccount.hpp"
#include "TransactionAccount.hpp"
/*
class Accounts
{
	private:
		std::vector <std::unique_ptr<TransactionAccount> > transAccounts;
		std::vector <std::unique_ptr<SavingsAccount> > savingsAccounts;
		std::vector <std::unique_ptr<LongTermSavingsAccount> > longTermAccounts;
		
		const unsigned int maxAccounts;
		
		const bool full() const;
		const bool empty() const;
	
		const size_t getAccountIndex(const unsigned int accountNo) const;
		
		Type const & operator[](const unsigned int idx) const;

	public:
		Accounts();
		const bool push_back(TransactionAccount a);
		const bool push_back(SavingsAccount a);
		const bool push_back(LongTermSavingsAccount a);
		
};

*/	

#endif